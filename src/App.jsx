import UserInputField from "./components/UserInputField.jsx";import {useState} from "react";import Results from "./components/Results.jsx";const INITIAL_INPUTS = {    initialInvestment: 1000,    annualInvestment: 1200,    expectedReturn: 6,    duration: 10};function App() {    const [inputs, setInputs] = useState(INITIAL_INPUTS);    function updateInputs(key, value) {        setInputs(prevInputs => {            return {                ...prevInputs,                [key]: +value // Convert the value to a number by adding a +            }        });    }    return (        <>            <div id="user-input">                <div className="input-group">                    <UserInputField label="Initial Investment" id="initial-investment"                                    value={inputs.initialInvestment}                                    onChange={(value) => updateInputs('initialInvestment', value)} step={1000}/>                    <UserInputField label="Annual Investment" id="annual-investment"                                    value={inputs.annualInvestment}                                    onChange={(value) => updateInputs('annualInvestment', value)} step={1000}/>                </div>                <div className="input-group">                    <UserInputField label="Expected Return" id="expected-return"                                    value={inputs.expectedReturn}                                    onChange={(value) => updateInputs('expectedReturn', value)}/>                    <UserInputField label="Duration" id="duration"                                    value={inputs.duration}                                    onChange={(value) => updateInputs('duration', value)}/>                </div>            </div>            <Results inputs={inputs}/>        </>    )}export default App